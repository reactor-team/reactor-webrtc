/target
Cargo.lock
# WebRTC build pipeline outputs (large) — never committed; published as
# versioned prebuilt artifacts instead (see webrtc-build/).
/webrtc-build/src/
/webrtc-build/out/
/webrtc-build/dist/
/webrtc-build/depot_tools/
*.a
*.lib
*.tar.gz
*.tar.zst
.DS_Store

# mise local state (mise.lock IS committed; these are per-machine overrides)
/.mise.local.toml
mise.local.toml
