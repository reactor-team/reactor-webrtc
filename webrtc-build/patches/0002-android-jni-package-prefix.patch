diff --git a/third_party/jni_zero/codegen/header_common.py b/third_party/jni_zero/codegen/header_common.py
--- a/third_party/jni_zero/codegen/header_common.py
+++ b/third_party/jni_zero/codegen/header_common.py
@@ -32,7 +32,22 @@
 #endif
 
 """)
+    # When a package prefix is active the generated C++ identifier changes
+    # (e.g. org_webrtc_Foo_clazz → inc_reactor_org_webrtc_Foo_clazz), but
+    # static .cc files in sdk/android/src/jni/ reference the unprefixed name
+    # directly.  Emit a compat #define so those files compile without edits.
+    if java_class._prefix:
+      original_fqn = java_class._fqn[len(java_class._prefix) + 1:]
+      original_escaped = common.jni_mangle(original_fqn)
+      if original_escaped != escaped_name:
+        sb(f"""\
+#ifndef {original_escaped}_clazz_defined
+#define {original_escaped}_clazz_defined
+#define {original_escaped}_clazz {escaped_name}_clazz
+#endif
+
+""")
 
 
 def class_accessor_expression(java_class):
   if java_class == java_types.CLASS_LOADER_CLASS:
     return 'jni_zero::g_class_loader_class'
diff --git a/third_party/jni_zero/jni_zero.gni b/third_party/jni_zero/jni_zero.gni
index 532b59115295..2713c942b6bf 100644
--- a/third_party/jni_zero/jni_zero.gni
+++ b/third_party/jni_zero/jni_zero.gni
@@ -27,6 +27,14 @@ declare_args() {
       (!enable_jni_multiplexing && !is_java_debug) || is_cronet_build
 }
 
+declare_args() {
+  # Reactor: package prefix for JNI-generated Java classes on Android.
+  # E.g. "inc.reactor" → org.webrtc.Foo becomes inc.reactor.org.webrtc.Foo.
+  # Maps to jni_zero.py's --package-prefix flag (same mechanism Cronet uses for
+  # "internal"). Empty string = no prefix (upstream default).
+  android_jni_package_prefix = ""
+}
+
 assert(!use_hashed_jni_names || !enable_jni_multiplexing,
        "Multiplexing requires that hashed names be disabled.")
 
@@ -282,6 +290,14 @@ template("generate_jni_registration") {
     if (defined(is_cronet_build) && is_cronet_build) {
       args += _cronet_renaming_extra_args
     }
+
+    # Reactor: apply our own Android package prefix when configured.
+    if (android_jni_package_prefix != "") {
+      args += [
+        "--package-prefix",
+        android_jni_package_prefix,
+      ]
+    }
   }
 }
 
@@ -505,6 +521,14 @@ template("generate_jni_impl") {
         if (defined(is_cronet_build) && is_cronet_build) {
           args += _cronet_renaming_extra_args
         }
+
+        # Reactor: apply our own Android package prefix when configured.
+        if (android_jni_package_prefix != "") {
+          args += [
+            "--package-prefix",
+            android_jni_package_prefix,
+          ]
+        }
       } else {
         if (is_robolectric) {
           not_needed(invoker, [ "jar_file" ])
